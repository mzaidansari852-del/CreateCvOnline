import { DE } from './de-copy';
import { landingShell } from '@/lib/i18n/landing';

/** The chrome around every German landing page — breadcrumb root, gallery, CTA. */
export const DE_SHELL = landingShell('de', DE);
