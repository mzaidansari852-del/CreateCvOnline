import { FR } from './fr-copy';
import { landingShell } from '@/lib/i18n/landing';

/** The chrome around every French landing page — breadcrumb root, gallery, CTA. */
export const FR_SHELL = landingShell('fr', FR);
