import { NL } from './nl-copy';
import { landingShell } from '@/lib/i18n/landing';

/** The chrome around every Dutch landing page — breadcrumb root, gallery, CTA. */
export const NL_SHELL = landingShell('nl', NL);
