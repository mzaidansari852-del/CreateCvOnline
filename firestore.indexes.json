{
  "$comment": "Composite indexes required by CreateCVOnline. Deploy with `npm run firebase:rules`. Firestore creates single-field indexes automatically; only the composite and collection-group ones below need declaring.",
  "indexes": [
    {
      "$comment": "getSharedCV(): collection-group lookup of a public CV by its share id.",
      "collectionGroup": "cvs",
      "queryScope": "COLLECTION_GROUP",
      "fields": [
        { "fieldPath": "shareId", "order": "ASCENDING" },
        { "fieldPath": "isPublic", "order": "ASCENDING" }
      ]
    },
    {
      "$comment": "listAllCVs(): the admin console's most-recently-edited feed across all accounts.",
      "collectionGroup": "cvs",
      "queryScope": "COLLECTION_GROUP",
      "fields": [{ "fieldPath": "updatedAt", "order": "DESCENDING" }]
    },
    {
      "$comment": "listAllPayments(): the admin payment ledger across all accounts.",
      "collectionGroup": "payments",
      "queryScope": "COLLECTION_GROUP",
      "fields": [{ "fieldPath": "createdAt", "order": "DESCENDING" }]
    },
    {
      "$comment": "revenueSummary(): completed payments across all accounts.",
      "collectionGroup": "payments",
      "queryScope": "COLLECTION_GROUP",
      "fields": [
        { "fieldPath": "status", "order": "ASCENDING" },
        { "fieldPath": "createdAt", "order": "DESCENDING" }
      ]
    }
  ],
  "fieldOverrides": []
}
